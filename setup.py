from setuptools import setup, find_packages

setup(
    name='elastic_multiple_scattering',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add your project's dependencies here
    ],
    entry_points={
        'console_scripts': [
            # Add command line scripts here
        ],
    },
)